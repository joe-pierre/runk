# BUGS CORRIGÉS

- **[2026-08-02]** Tâche 30 : l'ajout du champ de recherche intégré sur `HomeScreen` a rendu `find.byType(TextField)` ambigu dans deux tests préexistants qui tapaient dans un dialogue ouvert par-dessus (`bookmark_context_menu_test.dart` : "Modifier les tags" ; `bulk_selection_toolbar_test.dart` : "Ajouter un tag (N)") — corrigé en scopant la recherche à l'`AlertDialog` ouvert (`find.descendant(of: find.byType(AlertDialog), matching: find.byType(TextField))`), voir `DECISIONS.md`, entrée "Tâche 30 — `find.byType(TextField)` devenu ambigu" — `test/widget/features/bookmarks/presentation/bookmark_context_menu_test.dart`, `test/widget/features/bookmarks/presentation/bulk_selection_toolbar_test.dart`
- **[2026-08-02]** Tâche 29 : dette documentée depuis la Tâche 7 — les icônes SVG de plateforme (`assets/icons/x.svg`, `instagram.svg`, `facebook.svg`, `threads.svg`) restaient absentes de `BookmarkCard`, qui n'affichait que des `Icons.*` Material génériques pour les 6 plateformes — corrigé via un nouveau `_PlatformIcon` (câble les 4 SVG dédiés avec `flutter_svg`, teintés selon le thème actif ; YouTube/TikTok conservent leurs icônes Material, teintées à l'identique pour rester cohérentes) — voir `DECISIONS.md`, entrée "Tâche 29" — `lib/features/bookmarks/presentation/bookmark_card.dart`
- **[2026-08-02]** Tâche 27 : `AddBookmarkSheet` produisait un overflow ("BOTTOM OVERFLOWED BY 23 PIXELS") quand le clavier était ouvert et que `TagInputField` affichait plusieurs suggestions de tags — le `Column` du contenu de la modale n'avait aucun ancêtre défilant, seulement `MainAxisSize.min`, alors que la hauteur cumulée (miniature + titre + tags + jusqu'à 160px de suggestions + bouton) pouvait dépasser l'espace restant sous le clavier — corrigé en enveloppant ce `Column` dans un `SingleChildScrollView`, sans toucher à `TagInputField` (composant partagé avec le menu contextuel et la sélection groupée, voir `DECISIONS.md`, entrée "Tâche 27") — `lib/features/bookmarks/presentation/add_bookmark_sheet.dart`
- **[2026-08-02]** Tâche 25 (bonus, bug préexistant documenté en Tâche 23) : un tag porté uniquement par des bookmarks masqués (`isHidden: true`) restait visible dans `TagsScreen`/l'autocomplétion — `distinctTagsProvider` dérivait ses tags de *tous* les bookmarks retournés par `bookmarkListProvider`, sans filtrer `isHidden` — corrigé en limitant la dérivation aux bookmarks `isHidden == false`, en plus du filtrage des `TagEntity.isHidden == true` désormais appliqué par `TagRepository.getManagedTagNames()` (voir `DECISIONS.md`, entrée "Tâche 25 — Bonus") — `lib/features/tags/presentation/distinct_tags_provider.dart`, `lib/features/tags/data/tag_repository.dart`
- **[2026-08-01]** Tâche 19 : retour de test manuel — `ClipboardSuggestionBanner` (`MaterialBanner` par défaut, fond neutre, apparition instantanée) passait facilement inaperçue au retour au premier plan — corrigé sans la rendre bloquante (une popup centrée avait été explicitement écartée en amont, contredirait SPEC.md section 4 règle 7 et section 11) : fond `colorScheme.primaryContainer` distinct du reste de `HomeScreen`, apparition en glissement + fondu (`AnimatedSwitcher`, 300 ms) au lieu d'instantanée, retour haptique `HapticFeedback.lightImpact()` déclenché une seule fois par nouvelle suggestion via `ref.listen` (jamais à un rebuild) — texte et actions "Ajouter"/"Ignorer" inchangés (voir `DECISIONS.md`, entrée "Tâche 19 — Renforcement visuel...") — `lib/features/bookmarks/presentation/clipboard_suggestion_banner.dart`
- **[2026-08-01]** Tâche 18 : détection clipboard TikTok Lite silencieusement rejetée — `ClipboardService._isValidUrl` exigeait, comme `ShareIntentService` avant la Tâche 16, que l'intégralité du contenu du presse-papier soit une URL valide, alors que TikTok Lite copie un texte libre entourant le lien réel de texte promotionnel et d'un second lien non pertinent — corrigé en réutilisant la logique d'extraction de la Tâche 16, désormais factorisée dans un nouvel utilitaire partagé `UrlTextExtractor` (`lib/core/utils/url_text_extractor.dart`), appelé par `ClipboardService._checkClipboard` avant la vérification `SourceDetector.detect` (voir `DECISIONS.md`, entrée "Tâche 18") — `lib/core/services/clipboard_service.dart`, `lib/core/services/share_intent_service.dart` (délègue désormais au même utilitaire, comportement inchangé)
- **[2026-08-01]** Tâche 17 : `TwitterProvider` ne remontait jamais aucune miniature pour les posts X (choix assumé de la Tâche 7, voir `DECISIONS.md`) — sur demande explicite, ajout d'un scraping `og:image` complémentaire (`OgTagScraper`, timeout dédié 2s indépendant de l'appel oEmbed) après l'appel oEmbed réussi ; échec toujours silencieux (`thumbnailUrl` reste `null`, `isPartial` reste `false`), le titre restant exclusivement issu de l'oEmbed (voir `DECISIONS.md`, entrée "Tâche 17") — `lib/core/services/metadata/providers/twitter_provider.dart`
- **[2026-08-01]** Tâche 16 : partage depuis TikTok Lite silencieusement rejeté — `ShareIntentService._isValidUrl` exigeait que l'intégralité de la chaîne partagée soit une URL valide, mais TikTok Lite partage un texte libre entourant le lien réel de texte promotionnel et d'un second lien non pertinent (ex: `"... https://vm.tiktok.com/ZS4BB5Rc7/ ... https://www.tiktok.com/tiktoklite"`) — corrigé par une nouvelle méthode `_extractBestUrl` qui repère toutes les sous-chaînes ressemblant à une URL via regex, nettoie la ponctuation finale parasite, puis priorise l'URL dont `SourceDetector.detect` reconnaît une plateforme plutôt que la première trouvée dans l'ordre du texte (voir `DECISIONS.md`, entrée "Tâche 16") — `lib/core/services/share_intent_service.dart`
- **[2026-08-01]** Tâche 14 : retour de test manuel — libellé du bouton principal d'`AddBookmarkSheet` ("Enregistrer") incohérent avec le vocabulaire "Ajouter" déjà utilisé pour la même action dans `clipboard_suggestion_banner.dart`, et absence de bouton visible pour valider un tag tapé manuellement dans `TagInputField` (seule la touche clavier "Terminé"/Entrée le permettait, non découvrable) — corrigé en harmonisant le libellé sur "Ajouter" et en ajoutant un `IconButton.filled` (icône "+") à côté du champ de tags, appelant la même logique de validation que la soumission clavier (voir `DECISIONS.md`, entrée "Tâche 14") — `lib/features/bookmarks/presentation/add_bookmark_sheet.dart`, `lib/features/bookmarks/presentation/tag_input_field.dart`
- **[2026-08-01]** Tâche 12 : miniatures Instagram/Facebook affichant `broken_image_outlined` après redémarrage de l'app — `_MetadataPreview` (`add_bookmark_sheet.dart`) et `_BookmarkThumbnail` (`bookmark_card.dart`) chargeaient les images via `Image.network`, sans cache disque, alors que les URLs `og:image` de ces plateformes pointent vers des CDN signés à expiration courte — corrigé en remplaçant par `CachedNetworkImage` (package `cached_network_image`, cache disque géré par `flutter_cache_manager`, strictement local à l'appareil, voir `DECISIONS.md` entrée "Tâche 12") — icônes de repli (`videocam_off_outlined`/`broken_image_outlined`) inchangées — `lib/features/bookmarks/presentation/add_bookmark_sheet.dart`, `lib/features/bookmarks/presentation/bookmark_card.dart`
- **[2026-08-01]** Tâche 11 : titres illisibles remontant jusqu'à l'UI (`AddBookmarkSheet`, `BookmarkCard`, `HomeScreen`) pour les bookmarks Instagram/Facebook/Threads — `OgTagScraper._extractProperty` extrayait le contenu brut de l'attribut `content` sans décoder les entités HTML (`&quot;`, `&#x2014;`, `&#x4eca;`, etc.) systématiquement présentes dans le HTML source de ces plateformes — corrigé par décodage via `html_unescape` juste après extraction (voir `DECISIONS.md`, entrée "Tâche 11 — Décodage des entités HTML") — `lib/core/services/metadata/providers/og_tag_scraper.dart`
- **[2026-07-31]** Tâche 10 : `flutter test` complet bloqué indéfiniment (jamais dans `flutter test integration_test` ni dans `flutter analyze`) — `search_screen_test.dart` appelait des opérations Isar réelles (`createBookmark`, recherche via `bookmarkSearchProvider`) directement dans un corps `testWidgets`, or ces opérations se résolvent via un port natif (thread en arrière-plan) qu'`AutomatedTestWidgetsFlutterBinding` ne relaie jamais sans `tester.runAsync()` — voir `DECISIONS.md`, entrée "Tâche 10 — `flutter test` bloqué indéfiniment" — `test/widget/features/search/presentation/search_screen_test.dart`
- **[2026-07-30]** Tâche 6 : `ShareIntentGate` faisait planter toute l'app au démarrage (`MissingPluginException` non interceptée) sur toute plateforme sans canal natif `receive_sharing_intent` (desktop, ou iOS avant finalisation de la Share Extension dans Xcode) — corrigé par un `try/catch` journalisé autour de `ShareIntentService.initialize()` (voir `DECISIONS.md`) — `lib/features/bookmarks/presentation/share_intent_gate.dart`
- **[2026-07-30]** Conflit de versions bloquant `flutter pub add` de la chaîne Riverpod (génération de code) — `isar_generator` impose `analyzer <6.0.0`, incompatible avec `riverpod_generator` récent — résolu en figeant `flutter_riverpod`/`riverpod_annotation`/`riverpod_generator` en 2.x (voir `DECISIONS.md`) — `pubspec.yaml` — *devenu obsolète le [2026-07-30], voir migration `isar_community` ci-dessous*
- **[2026-07-30]** `flutter build apk` en échec (namespace manquant, NDK incohérent, cible JVM incohérente) sur des plugins tiers non maintenus (`isar_flutter_libs`, `receive_sharing_intent`, ...) — voir `DECISIONS.md` — `android/build.gradle.kts`, `android/app/build.gradle.kts`
- **[2026-07-30]** Migration `isar`→`isar_community` : lève la contrainte Riverpod 2.x (repasse en 3.x), rend inutile le correctif de `namespace` manquant, mais impose `minSdk 23` (perte du support Android < 6.0) et conserve les correctifs NDK/JVM (dus à `receive_sharing_intent`, sans lien avec Isar) — voir `DECISIONS.md`, entrée "Migration vers isar_community" — `pubspec.yaml`, `SPEC.md`, `android/build.gradle.kts`, `android/app/build.gradle.kts`
- **[2026-07-30]** Tâche 6.5 (tests `ClipboardSuggestion`) : un `ProviderContainer` de test avec `container.listen(...)` puis un seul `await Future<void>.delayed(Duration.zero)` ne suffisait pas toujours à laisser Riverpod propager la valeur résolue d'un `FutureProvider` dépendant jusqu'au `Notifier` qui le `watch` — l'abonnement au flux (`_listenTo`) pouvait ne pas être attaché à temps avant l'émission testée, faisant échouer le test de façon dépendante du timing exact (pas un bug de l'app : en usage réel, `ConsumerWidget.build` se réconcilie avec le cycle de frame Flutter). Corrigé en remplaçant ces attentes par `pumpEventQueue()` (de `flutter_test`, boucle plusieurs tours de la file d'événements) dans `test/unit/services/clipboard_service_test.dart` et `test/unit/features/bookmarks/presentation/clipboard_suggestion_provider_test.dart`.

- **[2026-08-01]** Tâche 15 (gestion indépendante des tags, `TagEntity`/`TagRepository`) : `flutter analyze` propre, `flutter test` (108 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10 ci-dessous) vérifiés en local, y compris de nouveaux tests unitaires (`test/unit/features/tags/data/tag_repository_test.dart`, 7 cas couvrant création/doublon/renommage propagé/renommage d'un tag dérivé/suppression en cascade) et widgets (`tags_screen_test.dart` étendu : création, renommage, suppression avec confirmation). `flutter test integration_test -d linux` et un lancement `flutter run -d linux` confirment que l'instance Isar partagée (`BookmarkEntitySchema` + `TagEntitySchema`, voir `DECISIONS.md`) s'ouvre sans erreur au démarrage réel de l'app. Aucun appareil physique Android/iOS ni émulateur disponible dans cette session (même limitation que les tâches précédentes) : le rendu réel des dialogues d'ajout/renommage/suppression et du menu contextuel par tag sur petit écran reste à valider visuellement par l'utilisateur.
- **[2026-08-01]** Tâche 23 : un bookmark masqué (section "My Eyes Only", Tâche 22) restait trouvable via `SearchScreen` en tapant un terme présent dans son titre ou ses tags — `BookmarkRepository.searchBookmarks` déléguait à `BookmarkLocalDatasource.searchByTitleOrTags` sans filtre `isHidden`, contournement direct de la protection par code — corrigé en excluant `isHidden: true` directement dans la requête Isar de `searchByTitleOrTags`, au même endroit que le filtre `isDeletedLocally` déjà en place (voir `DECISIONS.md`, entrée "Tâche 23") — `lib/features/bookmarks/data/bookmark_local_datasource.dart`

# ROADMAP (idées / améliorations futures)

## Monétisation
- Modèle freemium : gratuit jusqu'à un nombre limité de bookmarks, illimité en payant
- Abonnement mensuel (~2-5$/mois) pour la synchronisation cloud multi-appareils et des fonctionnalités avancées
- Alternative : achat unique de l'application sans palier gratuit

## Fonctionnalités futures
- **[2026-08-02]** Flux "mot de passe oublié" (Tâche 28) : hors périmètre explicite du prompt de la Tâche 28, non implémenté — un utilisateur qui oublie son mot de passe reste bloqué (pas de `resetPasswordForEmail`). Voir DECISIONS.md, entrée "Tâche 28 — Flux mot de passe oublié non implémenté".
- Supabase Realtime pour synchronisation instantanée multi-appareils (voir `SPEC.md` section 6, non implémenté en V1)
- Export des bookmarks (CSV / JSON) pour sauvegarde personnelle
- Partage d'une collection de bookmarks avec un autre utilisateur Runk (lecture seule)
- Détection de doublons (même URL déjà sauvegardée)
- Widget d'accueil (Android/iOS) affichant les derniers bookmarks ajoutés
- Version web ou tablette légère, en lecture, s'appuyant sur le même backend Supabase

## Points de vigilance techniques identifiés
- **[2026-08-02]** Tâche 31 (résolution de l'URL canonique TikTok, `canonicalUrl`) : `flutter analyze` propre, `flutter test -j 1` vert (suite complète, 2 skipped préexistants et sans rapport, voir entrée Tâche 10) — nouveaux cas dans `tiktok_provider_test.dart` (6 cas : sans redirection, redirection simple, plusieurs sauts, échec réseau silencieux, boucle de redirection bornée par `_maxRedirectHops`) et `bookmark_repository_test.dart` (4 cas, groupe « canonicalUrl (Tâche 31) », mapping aller-retour local/distant). `flutter pub run build_runner build --delete-conflicting-outputs` confirmé sans erreur pour le nouveau champ nullable `BookmarkEntity.canonicalUrl` (pas de migration Isar manuelle requise, voir DECISIONS.md). **Aucune vérification réseau réelle possible dans cette session** (même limitation que la quasi-totalité des tâches précédentes, pas d'accès réseau sortant vers `vm.tiktok.com`/`www.tiktok.com` depuis cet environnement) : tout le comportement de résolution de redirection n'a été vérifié qu'au niveau unitaire (mock HTTP via `MockClient`, redirections simulées) — à valider par l'utilisateur avec un vrai lien court TikTok avant de considérer le critère d'acceptation pleinement confirmé. **Action manuelle requise avant que la synchronisation distante de ce champ ne fonctionne :** exécuter `alter table bookmarks add column canonical_url text;` sur le projet Supabase (voir DECISIONS.md, entrée « Tâche 31 ») — tant que cette colonne n'existe pas, tout `insert`/`update` distant pour un utilisateur authentifié échoue silencieusement sur ce point précis (erreur Postgres 42703 interceptée comme un échec de synchronisation classique, le bookmark reste disponible localement avec `isSynced: false`, aucune perte de données).
- **[2026-08-02]** Tâche 30 (recherche intégrée à `HomeScreen`, suppression de `SearchScreen`) : `flutter analyze` propre, `flutter test -j 1` (183 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10) — `HomeScreen` réécrit en `ConsumerStatefulWidget`/`CustomScrollView`/`SliverAppBar(floating: true, snap: true)`, `lib/features/search/` et `test/widget/features/search/` supprimés, `router.dart`/`app_shell.dart` réduits à 2 branches/onglets (Home/Tags), `BookmarkSelectionScope.search` retiré. Un test widget hangait indéfiniment lors de la première rédaction (`pumpWidget` hors `runAsync` sur un écran accédant à Isar dès son premier `build()`, contrairement à l'ancien `SearchScreen` dont la requête vide court-circuitait Isar) — corrigé en reprenant le pattern déjà établi par `bookmark_context_menu_test.dart` (voir `DECISIONS.md`, entrée "Tâche 30 — Tests widgets avec Isar réel"). Aucun appareil physique Android/iOS ni émulateur disponible dans cette session : le rendu réel du masquage/réapparition de la barre au scroll et du clavier virtuel avec le champ de recherche dans le `SliverAppBar` reste à valider visuellement par l'utilisateur.
- **[2026-08-02]** Tâche 29 (système de thème, palette clair/sombre) : `flutter analyze` propre, `flutter test` (184 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10) — nouveaux fichiers `lib/core/theme/` (`app_palette.dart`, `app_color_tokens.dart`, `app_theme.dart`), `lib/features/settings/` (`theme_mode_provider.dart`, `theme_mode_selector.dart`). `flutter run -d linux` lancé pour vérifier l'absence de crash au démarrage avec le nouveau thème (aucune exception, arrêt propre). Aucun appareil physique Android/iOS ni émulateur disponible dans cette session (même limitation que la quasi-totalité des tâches précédentes) : le rendu réel des couleurs, des icônes de plateforme et de la bascule clair/sombre/système reste à valider visuellement par l'utilisateur avant de cocher la dernière ligne de `TODO.md` Phase 6.5.
- **[2026-08-02]** Tâche 28 (authentification Supabase, rattachement rétroactif des bookmarks locaux) : `flutter analyze` propre, `flutter test -j 1` (184 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10), `flutter test integration_test -d linux` vert. Nouveaux fichiers : `lib/features/auth/` (`auth_repository.dart`, `auth_repository_provider.dart`, `auth_failure.dart`, `auth_form.dart`, `link_local_bookmarks_prompt.dart`), `lib/app/app_drawer.dart`, `lib/core/services/app_scaffold_key_provider.dart` — 7 nouveaux tests (`bookmark_repository_test.dart`, groupe "authentification (Tâche 28)" : 4 cas couvrant `userId` renseigné à la création, `countLocalOnlyBookmarks`, `linkLocalBookmarksToUser` ; `auth_form_test.dart` : 3 cas widgets couvrant validation, affichage d'`AuthFailure`, bascule connexion/inscription). `BookmarkRepository` reçoit désormais un `getCurrentUserId` requis en constructeur (voir DECISIONS.md) — a nécessité la mise à jour de 11 fichiers qui le construisaient directement (10 dans `test/`, 1 dans `integration_test/app_flow_test.dart`) pour lui passer `getCurrentUserId: () => null` (comportement inchangé pour ces tests). `flutter run -d linux` lancé pour vérifier l'absence de crash au démarrage (Supabase + Isar s'initialisent, aucune exception non interceptée) — **non testé en interaction réelle** : ni la création de compte contre le vrai projet Supabase, ni l'ouverture du `Drawer`/l'affichage de la boîte de confirmation de rattachement, ni la synchronisation visible depuis un second appareil n'ont pu être vérifiés dans cet environnement (pas d'appareil physique Android/iOS ni d'émulateur, même limitation que la quasi-totalité des tâches précédentes). Reste à valider par l'utilisateur avant de considérer le critère d'acceptation de la Tâche 28 pleinement confirmé : créer des bookmarks hors ligne → créer un compte depuis la sidebar → vérifier l'apparition de la boîte de confirmation → accepter → vérifier la synchronisation depuis un second appareil connecté au même compte.
- **[2026-08-02]** Tâche 26 (sélection multiple, `BookmarkSelectionController`/`BulkSelectionToolbar`) : `flutter analyze` propre, `flutter test -j 1` (177 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10) — nouveaux fichiers `bookmark_selection_controller.dart`/`bulk_selection_toolbar.dart` (`lib/features/bookmarks/presentation/`), 9 nouveaux tests (`bookmark_selection_controller_test.dart` : 5 cas, `bulk_selection_toolbar_test.dart` : 4 cas widgets couvrant activation/annulation/suppression groupée/ajout de tag groupé) plus 4 nouveaux cas dans `bookmark_repository_test.dart` (`deleteBookmarks`/`addTagsToBookmarks`). `BookmarkRepository` reçoit désormais l'instance `Isar` en constructeur (voir `DECISIONS.md`) — a nécessité la mise à jour de 11 fichiers qui le construisaient directement (10 dans `test/`, 1 dans `integration_test/app_flow_test.dart`) pour lui passer `isar: isar`, chacun disposant déjà de cette instance dans son `setUp`. Aucun appareil physique Android/iOS ni émulateur disponible dans cette session (même limitation que la plupart des tâches précédentes) : le rendu réel de la case à cocher en superposition sur `BookmarkCard` et de `BulkSelectionToolbar` sur petit écran reste à valider visuellement par l'utilisateur.
- **[2026-08-02]** Tâche 25 (masquage de tag en cascade, `TagEntity.isHidden`) : `flutter analyze` propre, `flutter test -j 1` (164 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10), `flutter test integration_test -d linux` vert. Nouveaux fichiers : `hidden_tag_list_tile.dart`/`hide_tag_list_view.dart` (`lib/features/bookmarks/presentation/`), `hidden_tags_provider.dart` (`lib/features/tags/presentation/`) — 15 nouveaux tests au total (`tag_repository_test.dart` : 5, `bookmark_repository_test.dart` : 3, `distinct_tags_provider_test.dart` : 1, `hidden_tag_list_tile_test.dart` : 1, `hide_tag_list_view_test.dart` : 2, `my_eyes_only_screen_test.dart` : 2, `add_to_my_eyes_only_screen_test.dart` : 1). `BookmarkRepository` reçoit désormais un `TagLocalDatasource` requis en constructeur (lecture seule, voir `DECISIONS.md`) — a nécessité la mise à jour de 8 fichiers de test qui le construisaient directement (7 dans `test/`, 1 dans `integration_test/app_flow_test.dart`) pour ouvrir `TagEntitySchema` en plus de `BookmarkEntitySchema` et injecter un `TagLocalDatasource`, ainsi que 5 fakes `implements TagRepository` pour ajouter les nouvelles méthodes de l'interface (`getHiddenTagNames`/`hideTag`/`unhideTag`). **Limite documentée, non résolue** (voir `DECISIONS.md`) : impossible de distinguer un bookmark masqué individuellement (Tâche 24) d'un bookmark masqué via un tag — démasquer le tag démasque aussi le bookmark, même s'il avait été masqué individuellement par ailleurs. Aucun appareil physique Android/iOS ni émulateur disponible dans cette session : le rendu réel du `SegmentedButton` "Bookmarks"/"Tags" et de la section "Tags masqués" sur petit écran reste à valider visuellement par l'utilisateur.
- **[2026-08-01]** Tâche 24 (retrait du masquage du menu contextuel partagé, flux de sélection dédié) : `flutter analyze` propre, `flutter test` (149 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10) — nouveau `hidden_bookmark_menu_button.dart` (`lib/features/bookmarks/presentation/`, action locale "Ne plus masquer"/"Supprimer" pour `MyEyesOnlyScreen` uniquement) et nouvel écran `add_to_my_eyes_only_screen.dart` (sélection multiple par cases à cocher, bouton "Masquer la sélection"), tous deux couverts par de nouveaux tests widgets dédiés (`hidden_bookmark_menu_button_test.dart`, 3 cas ; `add_to_my_eyes_only_screen_test.dart`, 2 cas). `bookmark_context_menu_test.dart` mis à jour : le test vérifiant l'ouverture du menu contrôle désormais l'absence de "Masquer"/"Ne plus masquer" plutôt que leur présence, et un nouveau cas appelle `showBookmarkContextMenu` directement sur un bookmark déjà masqué pour prouver que le menu partagé ne les affiche jamais, quel que soit l'état du bookmark (le critère d'acceptation portait explicitement sur "aucun état de l'app", pas seulement les écrans qui filtrent déjà `isHidden`) — voir `DECISIONS.md`, entrées "Tâche 24". Ce retrait ne corrige pas la fuite `distinctTagsProvider` documentée dans l'entrée Tâche 23 juste en dessous (toujours non corrigée, toujours hors périmètre). Aucun appareil physique Android/iOS ni émulateur disponible dans cette session : le rendu réel du nouveau bouton "+" et de l'écran de sélection multiple sur petit écran reste à valider visuellement par l'utilisateur.
- **[2026-08-01]** Tâche 23 : `flutter analyze` propre, `flutter test` vérifiés en local (nouveaux cas dans `bookmark_local_datasource_test.dart` et `bookmark_search_provider_test.dart`, tous verts). **Fuite potentielle non corrigée dans cette tâche** (hors périmètre du prompt, qui ne portait que sur `searchBookmarks`) : `distinctTagsProvider` dérive ses tags de `bookmarkListProvider`, qui lit tous les bookmarks **sans filtre `isHidden`** (ce filtre n'existe que dans le `build()` de `HomeScreen`). Un tag qui n'existe que sur des bookmarks masqués reste donc visible dans `TagsScreen` et dans les suggestions de `TagInputField`, ce qui peut révéler l'existence d'un bookmark masqué (pas son contenu) à un utilisateur sans le code — voir `DECISIONS.md`, entrée "Tâche 23", pour le détail. À reprendre comme petite tâche dédiée si jugé prioritaire.
- **[2026-08-01]** Tâche 22 (section "My Eyes Only", `isHidden`) : `flutter analyze` propre, `flutter test` (141 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10) — nouveaux tests `test/unit/services/my_eyes_only_service_test.dart` (6 cas : `hasPinSet`/`setPin`/`verifyPin`/`resetPin`, code jamais stocké en clair), cas ajoutés à `bookmark_repository_test.dart` (mapping `isHidden` local/distant, `unhideAllBookmarks`), `home_screen_test.dart` (filtrage `isHidden`, appui long sur "Runk" sans code défini), `bookmark_context_menu_test.dart` (action "Masquer" persistée + retrait de `HomeScreen`), et nouveau `my_eyes_only_screen_test.dart` (2 cas). **Fait notable : le même appareil Android physique (`SCG10`) que la Tâche 19 était de nouveau connecté et accessible via `adb`/`flutter build apk --debug` durant cette session** (voir entrée Tâche 19 ci-dessous — ne pas supposer cette disponibilité acquise pour les sessions futures). A permis une validation manuelle complète du flux, réalisée directement sur les bookmarks réels de l'utilisateur déjà présents dans l'app (aucune donnée de test créée, aucune donnée perdue — le bookmark utilisé pour le test masquer/démasquer, tagué "test", a été restauré à l'identique et le code PIN de test a été effacé via "Code oublié ?" en fin de session, laissant l'app dans l'état "aucun code défini" antérieur au test) : appui long sur "Runk" → dialogue "Définir un code" (aucun code défini) → validation du contrôle "au moins 4 chiffres" et de la correspondance des deux champs → code défini → écran "My Eyes Only" vide → "Masquer" depuis le menu contextuel d'un bookmark → disparition immédiate de `HomeScreen` → réapparition dans "My Eyes Only" (réutilisant bien `BookmarkCard`) → "Ne plus masquer" depuis ce même écran → retour dans `HomeScreen` → "Code oublié ?" → confirmation → enchaînement automatique sur un nouveau dialogue de création de code (comportement confirmé par l'utilisateur en amont, voir `DECISIONS.md`). **Piège d'automatisation `adb` découvert et documenté pour les prochaines sessions utilisant ce device** : les captures d'écran (`adb exec-out screencap`) sont fournies à une résolution native (ici 1080×2400) mais affichées à l'agent à une résolution réduite (ici 900×2000, facteur ×1.2 indiqué dans chaque image) — utiliser les coordonnées affichées telles quelles avec `adb shell input tap`/`swipe` (sans les multiplier par ce facteur) fait manquer la cible réelle ; une frappe manquée sur un dialogue modal (`barrierDismissible` par défaut) l'a fermé silencieusement et déclenché un tap sur la carte de bookmark en dessous, ouvrant involontairement un post Instagram tiers — sans conséquence (aucune donnée modifiée), mais à surveiller. Toujours multiplier par le facteur d'échelle indiqué avant tout `adb shell input tap`/`swipe`, et vérifier par une capture d'écran après chaque interaction plutôt que d'enchaîner plusieurs taps à l'aveugle.
- **[2026-08-01]** Tâche 21 (menu contextuel par appui long, `bookmark_context_menu.dart`) : `flutter analyze` propre, `flutter test` (126 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10) — nouveau `test/widget/features/bookmarks/presentation/bookmark_context_menu_test.dart` (4 cas : ouverture du menu, modification des tags avec persistance vérifiée via le repository, annulation de suppression, suppression confirmée avec persistance vérifiée). Ce fichier de test a nécessité trois contournements supplémentaires (au-delà de `tester.runAsync`, déjà documenté Tâche 10) pour interagir de façon fiable avec `HomeScreen` sous `AutomatedTestWidgetsFlutterBinding` — voir `DECISIONS.md`, entrée "Tâche 21", pour le détail (`pumpAndSettle()` inutilisable dans `runAsync`, `tester.longPress()` dégénérant en tap simple dans `runAsync`, `pump()` sans `Duration` explicite ne faisant jamais progresser une transition de route). `BookmarkCard`/`onLongPress` non branché sur `SearchScreen` (hors périmètre du prompt de la Tâche 21, qui ne mentionne que `HomeScreen`) — à envisager comme petite tâche de cohérence si l'usage réel le justifie. Aucun appareil physique Android/iOS ni émulateur disponible dans cette session : le rendu réel du menu (bottom sheet, dialogues) sur petit écran reste à valider visuellement par l'utilisateur.
- **[2026-08-01]** Tâche 20 (bouton flottant "+", `ManualAddDialog`) : `flutter analyze` propre, `flutter test` (122 passed, 2 skipped préexistants et sans rapport, voir entrée Tâche 10) — nouveau `test/widget/features/bookmarks/presentation/manual_add_dialog_test.dart` (5 cas : erreur inline sur saisie invalide, fermeture avec URL validée, extraction depuis un texte libre collé, annulation, effacement de l'erreur à la modification) et un cas ajouté à `home_screen_test.dart` (tap sur le bouton flottant ouvre bien `ManualAddDialog`). Aucun appareil physique Android/iOS ni émulateur disponible dans cette session (même limitation que la plupart des tâches précédentes, voir entrées ci-dessous) : le rendu réel de la popup (clavier virtuel, position du bouton flottant par-dessus la liste et la bannière clipboard) reste à valider visuellement par l'utilisateur.
- **[2026-08-01]** Tâche 19 (renforcement visuel `ClipboardSuggestionBanner`) : `flutter analyze` propre, `flutter test` (115 passed, 2 skipped préexistants, voir entrée Tâche 10) — nouveau test widget dédié `clipboard_suggestion_banner_test.dart` (4 cas : absence de bannière, couleur d'accent, transition non instantanée, retour haptique déclenché une seule fois). Un échec supplémentaire est apparu en suite complète uniquement (`sync_service_test.dart`, `'start une reconnexion réseau déclenche une synchronisation'`), confirmé non lié à cette tâche : passe systématiquement en isolation, même famille de course contre une horloge réelle que le cas déjà `skip`-é juste en dessous dans le même fichier (voir entrée Tâche 10 ci-dessous) — non corrigé ici, hors périmètre du prompt de la Tâche 19, à reprendre avec le même test déjà `skip`-é. **Fait notable : un appareil Android physique (`SCG10`, Android 15) était connecté et accessible via `adb`/`flutter run` durant cette session** — exception à la limitation "aucun appareil physique disponible" documentée dans quasiment toutes les entrées précédentes de ce fichier (Tâches 3, 5, 6, 6.5, 7, 12, 12bis, 13, 15, 17). A permis une validation manuelle réelle : couleur d'accent visible en capture d'écran, `adb logcat` confirmant un unique appel `HapticFeedback` par nouvelle suggestion (vibration non ressentie car réglage système désactivé sur cet appareil, pas un défaut du code), "Ignorer" toujours sans effet de bord. Ne pas supposer cette disponibilité acquise pour les prochaines sessions — vérifier `flutter devices`/`adb devices` avant de s'appuyer dessus.
- **[2026-08-01]** Tâche 17 (`TwitterProvider`, scraping `og:image` complémentaire) : `flutter analyze` propre, `flutter test` (suite complète verte, 2 skipped préexistants sans rapport, voir entrée Tâche 10) — `twitter_provider_test.dart` étendu de 2 cas (miniature récupérée via mock ; échec silencieux). Vérification manuelle réseau réel (script Dart autonome, sans mock) : `https://x.com/i/status/2083336273922765201` (post avec média) retourne bien une miniature (`isPartial: false`) ; `https://x.com/jack/status/20` (post historiquement texte seul) retourne malgré tout un `og:image` non-`null`, mais correspondant à l'avatar de l'auteur plutôt qu'à un contenu spécifique du post — X ne sert donc pas systématiquement une absence totale d'image sur ce type de post, contrairement à l'hypothèse du prompt de tâche (voir `DECISIONS.md`, entrée "Tâche 17", pour le détail). Comportement du code jugé conforme (aucune demande de distinguer avatar de repli vs média du post) ; à surveiller si ce point s'avère gênant en usage réel (miniature non pertinente affichée pour un post texte). Aucun appareil physique Android/iOS disponible dans cette session : le rendu réel de la miniature dans `AddBookmarkSheet`/`BookmarkCard` sur petit écran reste à valider visuellement par l'utilisateur.
- **[2026-08-01]** Tâche 13 (`TagInputField`, autocomplétion des tags) : `flutter analyze` et `flutter test` (94 passed, 2 skipped préexistants, inchangé) vérifiés en local, y compris un nouveau test widget dédié (`test/widget/features/bookmarks/presentation/tag_input_field_test.dart`) couvrant l'affichage des suggestions par préfixe, l'ajout sans doublon au tap, et la non-duplication à la soumission d'un tag déjà présent. Aucun appareil physique disponible dans cette session (même limitation que les tâches précédentes) : le rendu réel du clavier virtuel et de la liste de suggestions sur petit écran reste à valider visuellement par l'utilisateur.
- **[2026-08-01]** Tâche 12bis (investigation User-Agent sur `OgTagScraper`) : hypothèse de départ — l'absence de header `User-Agent` sur la requête HTTP d'`OgTagScraper.scrape` expliquerait des miniatures manquantes dès l'ajout initial (pas seulement après redémarrage, sujet distinct de la Tâche 12) — **infirmée par le test**, avec un résultat inverse de celui attendu. Test manuel effectué (script Dart autonome exécutant directement `OgTagScraper` contre le réseau réel, sans mock) sur 5 liens Instagram publics :
  - `https://www.instagram.com/reel/DX7PnqbFL50/`
  - `https://www.instagram.com/reel/Dak0VgMFbZ-/`
  - `https://www.instagram.com/p/DXWBwBAE55Q/`
  - `https://www.instagram.com/p/DWwszBIE2Et/`
  - `https://www.instagram.com/p/DWm8OQKlKvC/`

  **Avant** (comportement actuel, sans header explicite) : `og:title` + `og:image` récupérés avec succès sur les **5/5** liens, y compris sous rafale de 8 requêtes rapprochées sur un même lien (aucun throttling observé).
  **Après** (ajout d'un `User-Agent` desktop Chrome/Windows récent) : `og:title` + `og:image` **absents sur les 5/5 mêmes liens** — Instagram sert alors sa page applicative React complète (`<title>Instagram</title>`, mur de connexion) plutôt que la page HTML statique simplifiée (contenant les balises `og:`) servie aux requêtes non identifiées comme un navigateur. Un contrôle immédiat (re-test avec le comportement "avant" juste après ces échecs) confirme que ce n'est pas du throttling — la même requête sans UA navigateur réussit de nouveau instantanément.

  **Conclusion : le header n'a pas été ajouté au code.** Le changement a été implémenté sur la branche `investigate/scraper-user-agent`, testé, puis annulé (`git checkout --`) après ce constat, pour ne pas introduire de régression sur le taux de récupération actuel. Voir `DECISIONS.md`, entrée "Tâche 12bis — Ne pas ajouter de User-Agent navigateur à `OgTagScraper`" pour l'analyse complète et la leçon retenue. **La cause réelle des miniatures manquantes dès l'ajout initial (hypothèse de départ de cette tâche) reste non identifiée** — à investiguer séparément (pistes possibles : conditions réseau/timeout réel sur device physique plutôt qu'environnement de dev, contenu réellement privé/supprimé/restreint géographiquement au moment du partage, ou variation du HTML servi par Instagram selon d'autres headers que le seul `User-Agent`).
- **[2026-08-01]** Tâche 12 (cache local des miniatures) : `flutter analyze` et `flutter test` (91 passed, 2 skipped, inchangé) vérifiés en local. Aucun appareil physique disponible dans cette session (même limitation que les tâches précédentes) : le critère d'acceptation exact (miniature Instagram encore affichée après un redémarrage réel, plusieurs heures après, URL CDN expirée) n'a pas pu être vérifié sur device — à valider par l'utilisateur avant de considérer la persistance réelle confirmée.
- **[2026-07-31]** Tâche 10 : une fois le blocage de `flutter test` levé (voir BUGS CORRIGÉS ci-dessus), deux échecs préexistants et sans rapport avec ce blocage sont apparus, jusqu'ici masqués par le hang qui empêchait la suite d'aller jusqu'au bout. Les deux tests sont marqués `skip` (voir chaque entrée ci-dessous pour la justification et le renvoi ici), **pas supprimés**, le temps d'être repris comme bugs dédiés — sans ce skip, `flutter test` resterait rouge en CI même sans régression volontaire, ce qui contredit le critère d'acceptation de la Tâche 10 (« une régression fait échouer la CI visiblement, avant tout merge possible » suppose une base verte) :
  - **Fichier :** `test/widget/features/tags/presentation/tags_screen_test.dart`
    **Test :** `'un tap sur un tag active le filtre puis revient sur Home'`
    **Symptôme observé :** échec déterministe, y compris isolé (`flutter test test/widget/features/tags/presentation/tags_screen_test.dart`) — `expect(container.read(bookmarkTagFilterProvider), 'cuisine')` échoue avec `Actual: <null>` après le tap sur le chip de tag, alors que le tag est bien affiché avant le tap (`find.text('cuisine')` trouve un widget).
    **Hypothèse de cause :** aucun rapport avec Isar (ce test n'utilise que `_FakeBookmarkList` et `GoRouter`, aucune donnée réelle). Le tap ne déclenche pas la mise à jour attendue de `bookmarkTagFilterProvider` — probablement une régression ou un bug latent introduit en Tâche 9 (filtrage par tag / navigation `TagsScreen` → `HomeScreen` via `GoRouter`), non détecté à l'époque car ce test n'avait jamais pu s'exécuter jusqu'au bout (masqué par le blocage `flutter test`, voir BUGS CORRIGÉS). Point de départ suggéré pour la reprise : vérifier le handler de tap dans `tags_screen.dart` (met-il bien à jour le provider avant ou après la navigation ?) et si `GoRouter.pop`/la navigation vers `/` recrée un `ProviderScope` différent de celui lu par le test.
  - **Fichier :** `test/unit/features/bookmarks/data/sync_service_test.dart`
    **Test :** `'le minuteur périodique déclenche une synchronisation'` (dans `group('start', ...)`)
    **Symptôme observé :** passe systématiquement en isolation (`flutter test ... --plain-name "minuteur périodique"`), mais échoue de façon intermittente quand la suite complète tourne dans l'ordre.
    **Hypothèse de cause :** relecture du test — il arme un `SyncService` avec `periodicInterval: const Duration(milliseconds: 20)` (un vrai `Timer.periodic`), puis attend un temps réel fixe (`Future<void>.delayed(const Duration(milliseconds: 100))`, soit seulement 5 intervalles de marge) avant de vérifier que la synchronisation a eu lieu. C'est une course contre une horloge réelle, pas un test déterministe : sous la charge CPU cumulée du reste de la suite (JIT, ouverture/fermeture répétée d'instances Isar dans les tests précédents), ce délai fixe peut ne pas suffire à laisser le minuteur se déclencher au moins une fois — pas nécessairement une pollution d'un test précédent (`Timer` non annulé) comme envisagé initialement, plutôt une hypothèse de timing insuffisamment robuste dès l'écriture du test. Piste de reprise suggérée : remplacer l'attente par un polling (`pumpEventQueue()` en boucle avec une condition de sortie, voir le pattern déjà utilisé dans `'une reconnexion réseau déclenche une synchronisation'` juste au-dessus) ou augmenter significativement la marge, plutôt qu'un délai fixe court.
- **[2026-07-30]** Tâche 5 (`BookmarkRepository`) : aucune synchronisation distante réelle n'est possible tant que l'authentification (Phase 6) n'existe pas — `BookmarkEntity.userId` reste `null` pour tout bookmark créé actuellement, la table Supabase `bookmarks` restera vide jusqu'à cette tâche. Comportement attendu et documenté (voir `DECISIONS.md`, entrée "user_id absent avant l'authentification"), pas un bug.
- Migration effectuée vers `isar_community`/`isar_community_generator` (fork actif du package `isar` v3 original, abandonné) — voir `DECISIONS.md`, entrée "Migration vers isar_community". Le projet nécessite désormais `minSdk 23` (Android 6.0+). **[2026-07-30] Décision produit tranchée : relèvement de minSdk 21 → 23 accepté définitivement.** Raison : Android 5.x (API 21-22) représente une part résiduelle et en déclin du marché actif (estimations entre <0,1 % et ~5 % selon les sources, données 2025-2026 — chiffre exact incertain mais tendance nettement à la baisse) ; le compromis est jugé raisonnable au vu du gain (dépendances Isar maintenues, retour à Riverpod 3.x) contre la perte de couverture, probablement marginale et concentrée sur des appareils très anciens peu compatibles avec les apps tierces ciblées par Runk (YouTube, Instagram, TikTok, etc.). `isar_community` étant lui aussi un projet plus jeune/moins établi que l'écosystème Flutter historique, surveiller son activité de maintenance dans la durée.
- Les schémas de deep link natifs (`instagram://`, `snssdk1233://` pour TikTok, etc.) ne sont pas documentés officiellement par les plateformes et peuvent changer sans préavis — prévoir une vérification périodique (voir `guide.md` section 7.5)
- Le scraping de balises `og:` pour Instagram/Facebook/Threads est fragile par nature : surveiller le taux de `is_partial = true` en production comme indicateur de santé de chaque provider
- **[2026-07-30]** Tâche 4 : les endpoints oEmbed YouTube/TikTok sont officiels mais leur format de réponse (noms de champs JSON) reste sous le contrôle des plateformes tierces et peut changer sans préavis — un échec de parsing bascule vers le fallback (`is_partial = true`) sans crasher l'app, mais surveiller les tests `youtube_provider_test.dart`/`tiktok_provider_test.dart` comme indicateur d'une éventuelle rupture de contrat
- Politique de conservation des données à clarifier si une fonctionnalité d'export ou de partage entre utilisateurs est ajoutée (RGPD si expansion vers l'UE)
- **[2026-07-30]** Tâche 6 (`AddBookmarkSheet`/`HomeScreen`/Share Intent) : `flutter analyze`, `flutter test` (24 tests) et `flutter build apk --debug` vérifiés en local ; l'app a aussi été lancée sur `flutter run -d linux` pour vérifier visuellement le flux (Supabase + Isar s'initialisent, `HomeScreen` affiche l'état vide, aucun crash). Aucun appareil Android/iOS physique ni émulateur disponible dans cette session (même limitation que Tâches 2 et 3) : le critère d'acceptation exact (partage YouTube réel → modale → sauvegarde → persistance après redémarrage) reste à valider manuellement sur device avant de cocher la dernière ligne de `TODO.md` Phase 4.
- **[2026-07-30]** Tâche 2 (`ShareIntentService` Android) : `flutter analyze`/`flutter test`/`flutter build apk --debug` vérifiés en local, mais le critère d'acceptation (partage réel depuis Instagram/TikTok sur appareil physique) n'a pas pu être testé manuellement dans cette session — à valider avant de cocher la ligne correspondante de `TODO.md` Phase 2
- **[2026-07-30]** Tâche 3 (Share Extension iOS) : session exécutée sur Linux, sans Xcode/macOS disponible — impossible de créer le target Xcode `RunkShareExtension`, d'activer la capability App Group, ou de lancer `pod install`/un build iOS depuis cette session (voir `DECISIONS.md`, entrée "Tâche 3 — Share Extension iOS"). Livré dans cette session : `ios/RunkShareExtension/Info.plist`, `ShareViewController.swift`, `Base.lproj/MainInterface.storyboard`, mise à jour de `ios/Runner/Info.plist`, et instructions manuelles détaillées dans `guide.md` section 4.2. Reste à faire sur une machine macOS avant de cocher la ligne correspondante de `TODO.md` Phase 2 : création effective du target dans Xcode, activation de l'App Group sur les deux targets, ajustement du `Podfile` généré, puis test réel de partage depuis Instagram/TikTok sur un appareil iOS physique
- **[2026-07-30]** Tâche 7 (`TwitterProvider`/`InstagramProvider`/`FacebookProvider`/`ThreadsProvider`) : `flutter analyze` et `flutter test` (54 tests) vérifiés en local. Aucun appareil Android/iOS physique ni émulateur disponible dans cette session (même limitation que les tâches précédentes) : le critère d'acceptation exact (partager un lien de chacune des 6 plateformes produit un bookmark valide, `isPartial` correctement positionné pour Facebook/Threads) n'a été vérifié qu'au niveau unitaire (mock HTTP par provider) — à valider manuellement sur device avant de cocher la dernière ligne de `TODO.md` Phase 5. Icônes de plateforme (`assets/icons/x.svg`, `instagram.svg`, `facebook.svg`, `threads.svg`) ajoutées mais non câblées dans `bookmark_card.dart` — voir `DECISIONS.md`, entrée "Tâche 7 — Icônes de plateforme".
- **[2026-07-30]** Tâche 6.5 (`ClipboardService`) : `flutter analyze`, `flutter test` (37 tests) et `flutter build apk --debug` vérifiés en local. `detectPatterns` (iOS 16+) non implémenté — nécessiterait un canal de plateforme Swift custom écrit dans Xcode, indisponible dans cette session (voir `DECISIONS.md`, entrée "Tâche 6.5 — `detectPatterns` iOS 16+"). La bannière système "Runk a collé depuis…" apparaîtra donc sur toutes les versions d'iOS tant que ce canal n'est pas développé sur une machine macOS — à reprendre si cela devient gênant en usage réel. Aucun appareil physique Android/iOS disponible dans cette session : le critère d'acceptation exact (copier un lien TikTok → bannière → "Ignorer" → jamais reproposée → priorité Share Intent) reste à valider manuellement sur device avant de cocher la dernière ligne de `TODO.md` Phase 4.5 (voir `guide.md` section 5.4).
- **[2026-07-30]** Tâche 8 (`DeepLinkService`) : `flutter analyze`, `flutter test` (tests unitaires dédiés + test widget de bout en bout simulant un tap sur `BookmarkCard`) et `flutter build apk --debug` vérifiés en local. Aucun appareil Android/iOS physique ni émulateur disponible dans cette session (même limitation que les tâches précédentes) : le critère d'acceptation exact (tap réel sur une vignette Instagram/TikTok avec l'app cible installée ou non) n'a été vérifié qu'au niveau unitaire (schémas natifs injectés, aucun appel de plateforme réel) — à valider manuellement sur device avant de cocher la dernière ligne ajoutée à `TODO.md` Phase 5. Déclarations de visibilité de paquets ajoutées (`android/app/src/main/AndroidManifest.xml` `<queries>`, `ios/Runner/Info.plist` `LSApplicationQueriesSchemes`) pour les 5 schémas natifs utilisés (Instagram, TikTok, Facebook, X, YouTube) — sans elles, `canLaunchUrl` échouerait silencieusement même app installée (voir `DECISIONS.md`, entrée "Tâche 8").
